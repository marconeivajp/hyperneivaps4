// -----------------------------------------------------------------
// ARQUIVO TEMPORÁRIO
// Criado apenas para o compilador (build.bat) não dar erro de falta de arquivo.
// Quando você for separar os códigos, apague isso e coloque o código real.
// -----------------------------------------------------------------

#include <stdint.h>

// Uma variável inútil apenas para o arquivo não ficar 100% vazio 
// e o compilador não soltar avisos (warnings) chatos.
int variavel_dummy_temporaria_apenas_para_compilar = 0;